# p83 Split Lane After Stage-1 Panel

This note is report-only. The new ligand stage-1 validation panel should not change split or leakage claims.

## Current Reading

- The panel is `complete` and `ready_for_operator_preview`.
- It validates only two ligand lanes: `P00387` and `Q9NZD4`.
- `P00387` is `aligned` and is the bulk-assay anchor.
- `Q9NZD4` is `aligned` but still candidate-only and bridge-rescue oriented.

## What Does Not Change

- Split claims do not change.
- Leakage claims do not change.
- No CV folds are unlocked or materialized.
- No ligand rows are materialized.
- No bundle ligand inclusion changes.

## Why The Claims Stay Stable

The split and leakage surfaces are still driven by the protein-spine previews and leakage-group/signature artifacts. The new ligand panel is advisory and lane-specific; it does not rewrite split assignment, leakage grouping, or any protected latest surfaces.

## Next Safe Follow-Up

After the panel is in place, the next safe operator move is to use it for lane steering only:

1. Keep `P00387` as the lead bulk-assay anchor.
2. Keep `Q9NZD4` as the bridge-rescue candidate.
3. Treat the panel as a preview aid, not a split or leakage authorization signal.

## Truth Boundary

This note does not authorize split progression, leakage recalculation, or ligand-row materialization. It only records that the stage-1 validation panel is operator-ready without changing the split/leakage truth.
