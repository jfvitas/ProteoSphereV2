# Operator Truth Review After Identity Core Preview

The operator truth surfaces remain aligned after `ligand_identity_core_materialization_preview` moved into the preview bundle.

What changed:
- The bundle validation now includes the four-row identity-core preview and marks it aligned.
- The operator queue still keeps `P00387` first, `Q9NZD4` as the bridge-rescue candidate, and `Q9UCM0` deferred.
- The support-readiness surface still says ligand rows are not materialized and bundle ligands are not included.

What remains true:
- The dashboard is still `blocked_on_release_grade_bar`.
- The operator status is still no-go.
- The preview remains report-only and does not certify canonical ligand rows.

Main caution:
- The bundle now carries a materialization-shaped preview name, so it is easy to overread it as real ligand materialization. It is not.
- Bundle inclusion is not the same as release readiness.

Bottom line:
- The operator surfaces and bundle surfaces are still aligned. The identity-core preview is now bundle-backed, but the candidate-only and non-materialized boundaries remain intact.
