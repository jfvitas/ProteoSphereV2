# Summary Library Operator Accession Matrix

- Accessions: `11`
- Variant-bearing accessions: `4`
- Structure-bearing accessions: `2`

## Rows

- `P00387` (`NADH-cytochrome b5 reductase 3`)
  variants `0`; structures `0`; action `protein_only_current_slice`; priority `observe`
  Useful protein-only summary exists, but no visible variant or structure slice exists yet.
- `P02042` (`Hemoglobin subunit delta`)
  variants `0`; structures `0`; action `protein_only_current_slice`; priority `observe`
  Useful protein-only summary exists, but no visible variant or structure slice exists yet.
- `P02100` (`Hemoglobin subunit epsilon`)
  variants `0`; structures `0`; action `protein_only_current_slice`; priority `observe`
  Useful protein-only summary exists, but no visible variant or structure slice exists yet.
- `P04637` (`Cellular tumor antigen p53`)
  variants `1439`; structures `0`; action `structure_followup_candidate`; priority `high`
  Variant-bearing accession with no visible structure-unit slice yet.
- `P09105` (`Hemoglobin subunit theta-1`)
  variants `0`; structures `0`; action `protein_only_current_slice`; priority `observe`
  Useful protein-only summary exists, but no visible variant or structure slice exists yet.
- `P31749` (`RAC-alpha serine/threonine-protein kinase`)
  variants `23`; structures `0`; action `structure_followup_candidate`; priority `high`
  Variant-bearing accession with no visible structure-unit slice yet.
- `P68871` (`Hemoglobin subunit beta`)
  variants `263`; structures `2`; action `candidate_only_no_variant_anchor`; priority `medium`
  Protein, variant, and structure slices are visible, but no direct structure-backed variant join is materialized.
- `P69892` (`Hemoglobin subunit gamma-2`)
  variants `0`; structures `0`; action `protein_only_current_slice`; priority `observe`
  Useful protein-only summary exists, but no visible variant or structure slice exists yet.
- `P69905` (`Hemoglobin subunit alpha`)
  variants `149`; structures `2`; action `candidate_only_no_variant_anchor`; priority `medium`
  Protein, variant, and structure slices are visible, but no direct structure-backed variant join is materialized.
- `Q2TAC2` (`Coiled-coil domain-containing protein 57`)
  variants `0`; structures `0`; action `protein_only_current_slice`; priority `observe`
  Useful protein-only summary exists, but no visible variant or structure slice exists yet.
- `Q9NZD4` (`Alpha-hemoglobin-stabilizing protein`)
  variants `0`; structures `0`; action `protein_only_current_slice`; priority `observe`
  Useful protein-only summary exists, but no visible variant or structure slice exists yet.

## Truth Boundary

- This matrix reports accession-level coverage across the currently materialized lightweight-library slices. It does not infer missing ligand or interaction families.
